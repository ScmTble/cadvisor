name: build

on:
  workflow_dispatch:

jobs:
  build:
    # ubuntu 22.04
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build
        run: |
          ./deploy/build.sh